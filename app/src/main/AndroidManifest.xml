<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.amikom.avent">

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBGQlakrkEAA_xV-79xs70L1Txh6RMXLF0" />

        <activity android:name=".feature.login.LoginActivity"
            android:theme="@style/AuthTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.register.SignUpActivity"
            android:theme="@style/AuthTheme"/>
        <activity android:name=".feature.profile.ProfileActivity" />
        <activity android:name=".main.BaseActivity" />
        <activity
            android:name=".main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".feature.editor.EventEditorActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".feature.placepicker.PlacePickerActivity" />
        <activity android:name=".feature.detail.DetailActivity"></activity>
    </application>

</manifest>